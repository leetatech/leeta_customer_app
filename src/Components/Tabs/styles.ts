import {StyleSheet} from 'react-native';
import {colors} from '../../Constants/Colors';

const createStyles = () =>
  StyleSheet.create({
    order_title: {
      fontWeight: '400',
      fontSize: 20,
      color: colors.BLACK,
      paddingLeft: 10,
      paddingBottom: 10,
    },
 
  });

export default createStyles;
