import {StyleSheet} from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    mainContainer:{
     justifyContent: 'center',
     alignItems: 'center',
    },
 
   
  });

export default createStyles;